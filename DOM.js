// DOM (Document Object Model) is a programming interface provided by the browser that allows 
// JavaScript to interact with and manipulate the structure, 
// style, and content of an HTML or XML document.


// window object 
// the window object represents an open window in a browser .
// it is browser objects not javascript & is automatically created by browser


// it is a global object with lots of properties & method


// Selecting with id 
// document.getElementById("myId")


//selecting with class 
// document.getElementsByClassName("myClass")


//selecting with tag
//document.getElementsByTagName("p")

//Query selector
//document.querySelector("myId/myClass/tag") .. returns first elements

//documents.querySelectorAll("myId/myClass/tag").. returns a nodelist


// properties
//tagName: returns tag for elements nodes
//innerText: returns the next content of the elements and all its children
//innerhtml; returns the plain text or html contents in the elements
//textContent: returns textual content even for hidden elements


// Dom Part 2*

// Attributes
// getattribute(attr)
// setAttribute(attr, value)

//style
// node.style

// insert elements (let el = document.createElement("div"))

// Node.append(el) //(add at the end of the node inside)
// node.prepend(el) //(adds at the start of node inside)
// Node.before(el)// add before the node outside
// // node.after(el)// adds after the node outside

// delete element
// node.remove () // remove the nodes

